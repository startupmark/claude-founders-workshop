Rename the `fmt` function to `formatNumber` in `lib/format.ts` and update its
callers so the app still typechecks. Keep the change scoped to the rename — do
not tidy or refactor anything else in the files you touch.
